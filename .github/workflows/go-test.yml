name: Go Tests CI

on:
  push:
    branches:
      - '**'  # Executa em qualquer branch
  pull_request:
    branches:
      - '**'  # Executa em qualquer PR

jobs:
  test:
    name: Testes Unitários Go
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        go-version: ['1.24']
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Go ${{ matrix.go-version }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
          cache: true

      - name: Verificar dependências
        run: |
          go mod download
          go mod verify

      - name: Executar go vet
        run: go vet ./...

      - name: Executar testes unitários
        run: go test -v -race -coverprofile=coverage.out ./...

      - name: Gerar relatório de cobertura
        run: go tool cover -func=coverage.out

      - name: Upload do relatório de cobertura
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.out
          retention-days: 30

      - name: Verificar cobertura mínima (opcional)
        run: |
          COVERAGE=$(go tool cover -func=coverage.out | grep total | awk '{print $3}' | sed 's/%//')
          echo "📊 Cobertura total: ${COVERAGE}%"
          
          # Definir cobertura mínima aceitável (ajuste conforme necessário)
          MIN_COVERAGE=0
          
          if (( $(echo "$COVERAGE < $MIN_COVERAGE" | bc -l) )); then
            echo "❌ Cobertura abaixo do mínimo de ${MIN_COVERAGE}%"
            exit 1
          else
            echo "✅ Cobertura acima do mínimo de ${MIN_COVERAGE}%"
          fi

  lint:
    name: Lint e Formatação
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: true

      - name: Verificar formatação do código
        run: |
          if [ "$(gofmt -s -l . | wc -l)" -gt 0 ]; then
            echo "❌ Código não está formatado corretamente. Execute 'gofmt -s -w .'"
            gofmt -s -l .
            exit 1
          else
            echo "✅ Código formatado corretamente"
          fi

      - name: Executar golangci-lint
        uses: golangci/golangci-lint-action@v4
        with:
          version: latest
          args: --timeout=5m

  build:
    name: Verificar Build
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: true

      - name: Build do projeto
        run: |
          go build -v ./cmd/edge-video
          echo "✅ Build executado com sucesso"

  summary:
    name: Resumo dos Testes
    runs-on: ubuntu-latest
    needs: [test, lint, build]
    if: always()
    
    steps:
      - name: Verificar status dos jobs
        run: |
          echo "📊 Resumo da execução:"
          echo ""
          echo "Testes: ${{ needs.test.result }}"
          echo "Lint: ${{ needs.lint.result }}"
          echo "Build: ${{ needs.build.result }}"
          echo ""
          
          if [ "${{ needs.test.result }}" == "success" ] && \
             [ "${{ needs.lint.result }}" == "success" ] && \
             [ "${{ needs.build.result }}" == "success" ]; then
            echo "✅ Todos os checks passaram com sucesso!"
            exit 0
          else
            echo "❌ Alguns checks falharam. Verifique os logs acima."
            exit 1
          fi
